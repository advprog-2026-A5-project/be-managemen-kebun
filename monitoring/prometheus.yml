global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "kebun-service"
    metrics_path: "/actuator/prometheus"
    static_configs:
      # Replace <KEBUN_HOST> with the deployed EC2 public DNS/IP for kebun.
      - targets: ["<KEBUN_HOST>:8081"]
